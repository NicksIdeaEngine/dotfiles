#!/bin/zsh

SOURCE_DIR="/home/efex/.local/kitty/kitty"
DEST_DIR="/mnt/sdc1/Dropbox/dots/ubuntu/.local/kitty/kitty"
DEST_FILE="$DEST_DIR/$1"

if [[ ! -f "$DEST_FILE" ]]; then
  mv "$SOURCE_DIR/$1" "$DEST_FILE"
  ln -s "$DEST_FILE" "$SOURCE_DIR"
fi

