set -g mouse on
set -g history-limit 10000
bind r source-file ~/.tmux.conf \; display "Reloaded!"
# source-file ~/.tmux.conf \; display "Reloaded!"
# new-session -d -s session1
# new-window -n vim
# attach-session -d -t session1
# select-window -t zsh
